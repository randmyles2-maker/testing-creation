import * as THREE from 'three';import {OrbitControls} from 'three/addons/controls/OrbitControls.js';import {GLTFLoader} from 'three/addons/loaders/GLTFLoader.js';const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,0.1,1000);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setSize(window.innerWidth,window.innerHeight);renderer.toneMapping=THREE.ACESFilmicToneMapping;document.body.appendChild(renderer.domElement);const light=new THREE.DirectionalLight(0xffffff,2);light.position.set(5,10,7.5);scene.add(light,new THREE.AmbientLight(0xffffff,0.8));const loader=new GLTFLoader();const loadAsset=(path,pos,scale=1)=>{loader.load(path,(gltf)=>{gltf.scene.position.set(...pos);gltf.scene.scale.set(scale,scale,scale);scene.add(gltf.scene)},undefined,(e)=>console.error(e))};loadAsset('assets/house_shell.glb',[0,0,0],5);loadAsset('assets/king_bed.glb',[-2,0,0]);loadAsset('assets/oled_tv.glb',[0,1,-4]);camera.position.set(0,2,10);const controls=new OrbitControls(camera,renderer.domElement);function animate(){requestAnimationFrame(animate);controls.update();renderer.render(scene,camera)}animate();
